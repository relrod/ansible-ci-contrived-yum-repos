# ansible-ci-contrived-yum-repos

These are yum repos for certain test cases in ansible/ansible, for `dnf` and
`yum` targets.

Contains sources/scripts only, no actual rpms or repodata, except in weird
circumstances.
